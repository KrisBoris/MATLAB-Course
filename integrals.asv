% Integrals
clc, clearvars, close all, format compact

syms x z    % Symbolic Math Toolbox variables
y = x.^2 + z;   % Y is function of X and Z
j = int(y, x);  % integral of input1 with respect to input2
myFun = matlabFunction(j)